[package]
name = "ls"
version = "0.1.0"
edition = "2024"

[dependencies]
clap.workspace = true
terminal_size = "0.4"
time = { version = "0.3", features = ["local-offset", "formatting", "macros"] }

[dev-dependencies]
tempfile = "3"